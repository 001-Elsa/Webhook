global:
  scrape_interval: 5s
  evaluation_interval: 15s
rule_files:
  - /etc/prometheus/rules/*.yml
scrape_configs:
  - job_name: eventrelay
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["notification-platform:8080"]

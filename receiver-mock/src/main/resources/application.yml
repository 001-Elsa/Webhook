server:
  port: 8082

spring:
  application:
    name: receiver-mock

management:
  tracing:
    sampling:
      probability: ${OTEL_SAMPLING_PROBABILITY:1.0}
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT:http://localhost:4318/v1/traces}

receiver:
  webhook-secret: ${RECEIVER_WEBHOOK_SECRET:}
